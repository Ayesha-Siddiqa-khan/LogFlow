apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: logflow
  labels:
    app: dashboard
    app.kubernetes.io/part-of: logflow
spec:
  type: NodePort
  selector:
    app: dashboard
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
